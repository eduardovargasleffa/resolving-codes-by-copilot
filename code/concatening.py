# We will receive two differents pieces of data from the user and concatenate them into a single string.

data1 = input('Write something: ')
data2 = input('Write something else: ')

# concatening data
data = data1 + ' ' + data2
print('The concatenated data is: ', data)
